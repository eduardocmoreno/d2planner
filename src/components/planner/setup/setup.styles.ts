import styled from "styled-components";

export const SkillPointsWrapper = styled.div`
  text-align: center;
  strong {
    margin: 0 0.15em;
    color: var(--color-gold);
    font-size: 3rem;
    font-family: var(--font-family-main);
  }
`;